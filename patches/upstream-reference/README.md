# Inactive upstream patch

The unchanged rc.9 patch is retained here with a `.patch.reference` extension for upstream comparison only. It implements newsletter/media behavior and verbose payload debugging, neither of which belongs to the text-only pilot. It must not be applied to the patched Baileys rc14 runtime. The reference extension prevents patch-package's recursive discovery from attempting to apply it.
