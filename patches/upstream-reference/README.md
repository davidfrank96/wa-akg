# Inactive upstream patch

The unchanged rc.9 patch is retained here for upstream comparison only. It implements newsletter/media behavior and verbose payload debugging, neither of which belongs to the text-only pilot. It must not be applied to the patched Baileys rc14 runtime. Keeping it out of the patches root prevents patch-package from applying it automatically.
